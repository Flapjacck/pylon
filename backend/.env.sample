# Environment configuration for Terraria Server Manager backend

# API Server Settings
API_TITLE=dopeApp
API_HOST=0.0.0.0
API_PORT=8000

# CORS Settings
# Add frontend URL(s) that are allowed to make requests to this API
# For Vite dev server: http://localhost:5173
CORS_ORIGINS=["http://localhost:5173"]
